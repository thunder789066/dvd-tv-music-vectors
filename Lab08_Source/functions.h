#pragma once
#include <iostream>
#include <vector>
#include <fstream>
#include <string>
using namespace std;

void sort_alg(vector<double> source);

double get_medium(vector<double> source);

double get_total(vector<double> source);

double get_mean(vector<double> source);
